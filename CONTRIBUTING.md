# 参加ガイド：自分の提案をサイトに反映するまで

このガイドは、**GitHubを初めて使う人向け**です。
みんなそれぞれ自分のAI（Claude・ChatGPT・Gemini など）と議論して改善案を考えているはずなので、
その案を **「提案 → テスト → 確認 → 反映」** の順でサイトに入れる手続きをまとめました。

- サイト（本番）：https://food-price-viz.vercel.app
- リポジトリ：https://github.com/kenisehu/food-price-viz
- 共通ルール：[AGENTS.md](AGENTS.md)（AIで作業する前に必ずAIに読ませる）

---

## 0. 全体の流れ（まずこれだけ覚える）

```
① 提案する        Discussions に「こう変えたい」を書く
② 担当を決める    Issue を作って「自分がやります」と宣言
③ 作る            自分用のブランチ（作業場所）で、AIに変更させる
④ テストする      自分のPCかプレビューURLで、壊れていないか確認
⑤ PRを出す        「この変更を本番に入れていい？」とみんなに聞く
⑥ 確認してもらう  別の人（できれば別のAIを使う人）が見て承認
⑦ 反映する        マージ → 数分で本番サイトに自動反映
```

**本番（main）には直接入れられません。** 必ず ⑥ で誰か1人の承認が必要です。
だから、途中で失敗しても本番サイトは壊れません。安心して試してください。

### 言葉の説明

| 言葉 | 意味 |
|---|---|
| リポジトリ | サイトのコードとデータが全部入った箱 |
| main | 本番サイトになっている「正式版」 |
| ブランチ | main をコピーした自分用の作業場所。ここで何をしても本番には影響しない |
| コミット | 変更を保存すること（セーブポイント） |
| push | 自分のPCの変更を GitHub に送ること |
| PR（プルリクエスト） | 「自分のブランチの変更を main に入れてください」というお願い |
| マージ | PR を main に取り込むこと。本番に反映される |
| プレビューURL | PR ごとに自動で作られる「その変更を入れたらこうなる」お試しサイト |

---

## 1. あなたはどのタイプ？

やることは同じですが、手の動かし方が変わります。自分に近い方を読んでください。

| タイプ | 使っているもの | できること |
|---|---|---|
| **A：AIが自分のPCで動く人** | Claude Code / Codex CLI / Gemini CLI など | ①〜⑦ 全部。AIにコマンドを打たせられる |
| **B：ブラウザでAIを使う人** | ChatGPT / Gemini / Claude のブラウザ版 | ①②⑥ は同じ。③〜⑤ は GitHub の画面で行う（小さい変更向き） |

**迷ったら、①の提案だけでも大歓迎です。** 実装はタイプAの人が引き受けられます。

---

## 2. 最初に1回だけやること

### 全員

1. GitHub から届いた招待メールの **「Accept invitation」** を押す
   （見当たらなければ https://github.com/kenisehu/food-price-viz/invitations ）
2. リポジトリのページ右上の **「Watch」→「All Activity」** を押す（PRやコメントの通知が届くようになる）

### タイプA（AIが自分のPCで動く人）

必要なもの：`git`、`gh`（GitHub CLI）、`Python 3.10以上`。
入っているか分からなければ、AIに「git と gh と python が入っているか確認して、無ければ入れ方を教えて」と頼んでください。

自分のAIに、こう頼みます：

```
次の作業をして。
1. gh auth login で GitHub にログインする（ブラウザでの承認は私がやる）
2. https://github.com/kenisehu/food-price-viz を clone する
3. AGENTS.md と CONTRIBUTING.md を読んで、このリポジトリのルールを要約して
4. python -m http.server 8765 --directory site でサイトを起動し、http://localhost:8765 を開けるか確認して
```

### タイプB（ブラウザでAIを使う人）

1. 自分のAIの「プロジェクト」や「カスタム指示」（ChatGPTならProject、GeminiならGem）に、
   [AGENTS.md](AGENTS.md) の中身を貼り付けておく
2. これで、AIがこのリポジトリのルールを守った提案をしてくれるようになる

---

## 3. 手順の詳細

### ① 提案する（全員・Discussions）

1. 自分のAIと議論して、改善案をまとめる
2. https://github.com/kenisehu/food-price-viz/discussions を開き、**「New discussion」→「Ideas」** を選ぶ
3. フォームの欄を埋めて投稿する（何を・なぜ・どのページで・考えを変える条件）
4. ほかの人の提案には、賛成なら 👍、意見があれば返信する

**AIへの頼み方の例：**

```
このサイト（https://food-price-viz.vercel.app）の改善案を、Discussionsのフォームに合わせて整理して。
・変えたいこと（1つだけ）
・なぜ変えたいか（誰が何に困っているか）
・どのページのどこか
・この案への一番強い反論と、それでもやる理由
数字や事実を新しく書くときは、必ず出典のURLを付けて。
```

> **1つの提案につき、変えることは1つ。** 「ランキングを直して、色も変えて、ページも足す」は3つに分けてください。
> 小さいほど、確認もマージも早く終わります。

### ② 担当を決める（全員・Issues）

提案に 👍 が付いて「やろう」となったら：

1. **Issues →「New issue」** で、提案を1行で書いた Issue を作る（本文に Discussion のリンクを貼る）
2. 右側の **「Assignees」** で自分を選ぶ ＝「自分がやります」の宣言
3. 同じことを2人がやってしまうのを防ぐため、**Issue に担当者が付いているものには手を出さない**

---

### ③〜⑤ 作る・テストする・PRを出す

#### タイプA（AIが自分のPCで動く人）

**③ 作る** ── AIにこう頼みます（`#12` は自分の Issue 番号に、`siromi` は自分の名前に置き換え）：

```
Issue #12 の変更をしたい。
1. main を最新にしてから、ブランチ siromi/issue-12 を作って
2. AGENTS.md のルールに従って変更して。変更は Issue に書いたことだけにして、ほかは触らないで
3. データ（site/data）を変える必要がある場合は、手で数字を書かず scripts/ のスクリプトで作って
4. 何をどう変えたか、ファイルごとに説明して
```

**④ テストする** ── AIにサイトを起動してもらい、**自分の目で**次を確認します：

```
python -m http.server 8765 --directory site
→ ブラウザで http://localhost:8765 を開く
```

- [ ] 食料トップ（`#/`）、サブカテゴリ（例 `#/c/0003`）、品目（例 `#/i/1001`）の3種類のページが表示される
- [ ] 変えたかった部分が、狙いどおりに変わっている
- [ ] 変えていない部分が壊れていない（グラフ・期間ボタン・並べ替え・検索）
- [ ] ブラウザの幅を狭くしても（スマホ幅）崩れない
- [ ] 右上の ◐ でダークモードにしても読める
- [ ] データを作り直した場合、`python -X utf8 scripts/build_cpi.py` がエラーなく終わる

AIに「ブラウザの開発者ツールのコンソールにエラーが出ていないか確認して」と頼むのも有効です。

**⑤ PRを出す** ── 確認できたら、AIにこう頼みます：

```
変更をコミットして、ブランチを push して、PR を作って。
PR の本文はテンプレートの欄を全部埋めて。
「使ったAI」には（例：Claude Code / Claude Opus 5）と書いて。
「自分で確認したこと」には、私がさっき確認したチェック項目を書いて。
```

#### タイプB（ブラウザでAIを使う人）

GitHub の画面だけで、ファイルを直接編集してPRを出せます。**1〜2ファイルの小さな変更向き**です。

**③ 作る**

1. 自分のAIに、変更したいファイルの中身を渡して直してもらう
   - ファイルの中身は、リポジトリでファイル名をクリック → 右上の **コピーボタン（Copy raw file）** で取れる
   - 頼み方の例：「このファイルを、Issue #12 の内容どおりに直して。変更は最小限にして、直した後のファイル全体を出して」
2. GitHub で同じファイルを開き、右上の **鉛筆アイコン（Edit this file）** を押す
3. 中身を全部消して、AIが出した内容を貼り付ける
4. 右上の **「Commit changes...」** を押す
5. 出てきた画面で次のように入力する
   - 変更の説明（1行）：例「上昇率ランキングから分類を除く（#12）」
   - **「Create a new branch for this commit and start a pull request」を選ぶ**（ここ大事。main を選ぶ欄はありません）
   - ブランチ名：`自分の名前/issue-12` など
6. **「Propose changes」** → 次の画面で **「Create pull request」**

**④ テストする** ── PR を作ると、数分で **Vercel のボットがPRにコメント**を付けます。
コメント内の **「Preview」のリンク** を開くと、あなたの変更が入ったお試しサイトが見られます。
上のタイプAのチェック項目と同じことを、そのサイトで確認してください。

> プレビューが開けない（ログイン画面になる）場合は、管理者（くじら）に連絡してください。
> 自分で確認できない場合は、PR本文に「プレビュー未確認」と書き、レビューする人に確認をお願いしてOKです。

**⑤ PRの本文を埋める** ── PR 画面の本文にテンプレートが出ているので、全部の欄を埋めます。

---

### ⑥ 確認してもらう（レビュー）

#### PRを出した人

1. PR の右側 **「Reviewers」** で、レビューしてほしい人を選ぶ
   **できれば自分と違うAIを使っている人**を選ぶ（同じAIは同じ間違いを見逃しやすいので）
2. 指摘をもらったら、同じブランチを直す
   - タイプA：AIに「PR #15 のレビューコメントに対応して、同じブランチに push して」
   - タイプB：PR の **「Files changed」** タブ → ファイル右上の「…」→「Edit file」で直してコミット
3. 直したら、指摘コメントに「直しました」と返信し、**「Resolve conversation」** を押す
   （未解決のコメントが残っているとマージできない設定です）

#### レビューする人

1. PR の **「Files changed」** タブで変更内容を見る
2. **プレビューURL**（Vercelのコメント）で実際に触ってみる。④のチェック項目を自分でもやる
3. 自分のAIに見てもらうのもOK：
   ```
   PR https://github.com/kenisehu/food-price-viz/pull/15 をレビューして。
   ・PR本文の目的どおりの変更か
   ・目的と関係ない変更が混ざっていないか
   ・AGENTS.md のルール（数字を手で書かない等）を守っているか
   ・壊れそうな箇所はないか
   指摘は「必ず直すべき」と「できれば」に分けて。
   ```
4. 右上の **「Review changes」** から結果を選ぶ
   - 問題なし → **「Approve」**
   - 直してほしい → **「Request changes」**（何をどう直してほしいか具体的に書く）
   - 質問だけ → **「Comment」**

**AIの指摘をそのまま貼るのではなく、自分で納得した指摘だけを書いてください。**

---

### ⑦ 反映する（マージ）

**マージするのは、PRを出した本人**です（承認をもらった後）。

1. PR の下の方で、緑のチェック（承認済み・Vercelのビルド成功）がそろっているのを確認
2. **「Squash and merge」→「Confirm squash and merge」** を押す
3. 数分待って、本番 https://food-price-viz.vercel.app で変更が反映されたか確認する
4. 元の Discussion に「PR #15 で反映しました」と書いて、Issue を閉じる
   （PR本文に `Closes #12` と書いておくと、マージ時に Issue が自動で閉じます）

ブランチはマージ後に自動で削除されます。

---

## 4. 困ったとき

| 状況 | どうする |
|---|---|
| **「This branch has conflicts」と出てマージできない** | 同じ場所を他の人が先に変えた状態。タイプAはAIに「main の最新を取り込んで、コンフリクトを解消して push して」。タイプBは PR 画面の「Resolve conflicts」を押すか、無理なら PR にコメントしてタイプAの人に助けを求める |
| **「Update branch」ボタンが出ている** | main が先に進んでいるだけ。押せば最新が取り込まれる |
| **マージしたら本番が壊れた** | 慌てずに、マージ済みの PR の下にある **「Revert」** を押す → 元に戻す PR ができるので、それを承認・マージする |
| **Vercelのコメントが「Failed」** | コメント内のリンクからログを開き、AIにエラーを読ませる |
| **Vercelのコメントに「Authorization required」** | 管理者（くじら）に連絡する |
| **自分の変更を途中でやめたい** | PR を「Close pull request」で閉じるだけでOK。本番には何も影響しない |
| **何をすればいいか分からない** | Discussions の「Q&A」に質問を書く |

---

## 5. やってはいけないこと

- **数字を手で書き換える**（公式データ → `scripts/` のスクリプト、の経路以外で `site/data` を変えない）
- **1つのPRに複数の提案を混ぜる**
- **自分のPRを、レビューを受けずにマージする**
- **パスワード・APIキー・個人情報をコミットや投稿に書く**（公開リポジトリなので、世界中から見えます）
- **AIが言ったことを確認せずに事実として書く**（出典URLを付ける）

---

## 付録：1回分の具体例

しろみさん（ChatGPTブラウザ版）が「上昇率ランキングに『米類』のような分類が混ざるのをやめたい」と思った場合：

1. ChatGPT と議論 → Discussions の Ideas に投稿 → かもみさんとsaya-tanuさんが 👍
2. Issue #12「上昇率ランキングから分類を除く」を作り、Assignees に自分を設定
3. ChatGPT に `site/app.js` を渡して直してもらい、GitHub の鉛筆ボタンで貼り付け →
   「Create a new branch…」を選んでブランチ `siromi/issue-12` → PR #15 を作成、本文に `Closes #12`
4. Vercel のコメントのプレビューURLで、ランキングから「米類」が消えたことを確認
5. Reviewers に（Claude Code を使う）くじらを指定
6. くじらがプレビューで確認し、Claude Code にもレビューさせて「Approve」
7. しろみさんが「Squash and merge」→ 本番で反映を確認 → Discussion に「反映しました」
